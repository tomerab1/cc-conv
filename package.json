{
  "name": "cc-bridge",
  "version": "0.1.0",
  "description": "Connect Claude Code sessions: a CC-to-CC channel bridge, a Telegram control channel, and a PreToolUse safety firewall.",
  "type": "module",
  "license": "MIT",
  "author": "tomerab1",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "cc-bridge": "./bin/cc-bridge.mjs",
    "cc-firewall": "./bin/cc-firewall.mjs"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'test/**/*.test.ts'",
    "set-token": "node scripts/set-token.mjs"
  },
  "keywords": [
    "claude-code",
    "mcp",
    "channels",
    "bridge"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
